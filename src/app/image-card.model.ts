/**
 * @description Interfaz que define la estructura de una tarjeta de imagen
 * Representa los datos necesarios para mostrar una imagen en la galería, incluyendo la fuente de la imagen (URL) y su texto alternativo.
 */
export interface ImageCard {
  imageSrc: string;
  imageAlt: string;
}
